<section>
    <h3><%= @artist.name %></h3>
        <ul>
            <% @artist.songs.each do |s| %>
                <%= link_to s.title, song_path(s) %>
             <% end %>
        </ul>
</section>