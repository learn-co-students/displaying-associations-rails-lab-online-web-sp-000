<h1>Artists Index</h1>

<ul>
  <% @artists.each do |artist| %>
    <li> <%= link_to artist.name, song_path(artist) %>, <%= "#{artist.song_count} #{pluralize(artist.song_count, "song")}" %> </li>
  <% end %>
</ul>
