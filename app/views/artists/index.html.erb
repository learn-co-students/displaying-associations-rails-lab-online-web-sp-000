<h1>Artist's Songs</h1>

<% @artists.each do |a|%>
  <h2>Artist Name: <%= a.name%></h2>
    <p><%= a.songs.count%> songs</p>
    <% a.songs.each do |s|%>
      <ul>
        <li>Song Title: <a href="/songs/<%= s.id%>"><%= s.title%></a></li>
      </ul>
    <% end %>
<% end %>