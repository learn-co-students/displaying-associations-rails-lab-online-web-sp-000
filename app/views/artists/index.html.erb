index

<ul>
  <%= @artists.each do |artist| %>
    <li>
      <%= link_to artist.name, artist_path(artist) %>
    </li>
    <li> This artist has <%=pluralize(artist.song_count, "song")%></li>
  <% end %>
</ul>
