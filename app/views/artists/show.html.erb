<h1><%= @artist.name %></h1>

<h3>Has <%= pluralize(@artist.song_count, 'Song') %></h3>
<ul>
    <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
    <% end %>
</ul>