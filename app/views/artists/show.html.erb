<!--links to the artist's songs by title -->
<!-- <link_to @artist.songs.title> -->

<h1><%= @artist.name %></h1>
 
<h2>Songs:<h2>
    <ul>    
        <% @artist.songs.each do |song| %>
            <li><%= link_to song.title, song_path(song)%></li>
        <% end %>
    </ul>

    <p><%= link_to "See all songs", songs_path %>
