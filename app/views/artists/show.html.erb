<h2><%= @artist.name %></h2>

<h4><%= pluralize(@artist.songs.count, "Song") %></h4>
<ul>
   <% @artist.songs.each do |song| %>
      <li> <%= link_to song.title, song_path(song) %></li>
   <% end unless @artist.songs.empty? %>
</ul>