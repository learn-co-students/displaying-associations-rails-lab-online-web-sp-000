
<h1> <%= @artist.name %> <%= @artist.song_count %> songs </h1>

<p> <%= @artist.song_count %> songs </p>
<ul> <%= @artist.song_count %> songs
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>