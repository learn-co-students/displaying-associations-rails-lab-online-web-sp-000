<title>Artist Page</title>

<h1><%= link_to @artist.name, artists_path(@artist) %></h1>
<br />
<br />
<h2>Songs:</h2>
<ul>
<% @artist.songs.each do |song| %>
<li>
<%= link_to song.title, song_path(song) %>
</li>
<% end %>
</ul>
