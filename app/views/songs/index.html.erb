<h1> All Songs </h1>

<ul>
  <%  @songs.each do |s| %>

  <h3> <li> <%= link_to " #{s.artist.name} - #{s.title}",song_path(s)%></li> </h3>
  <%# <% binding.pry %> %>
  <%= link_to "#{s.artist.name}", artist_path(s.artist) %>
  <% end%>
  </ul>
