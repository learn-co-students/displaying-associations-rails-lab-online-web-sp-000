<h1>All songs</h1>

<h3>
    <ul>
        <% @songs.each do |s| %>
            <li>
            <a href="<%= song_path(s) %>">
            <%= s.artist_name %>
            -
            <%= s.title %>
            </a>
            </li>
        <% end %>
    </ul>
</h3>